const EditItems = ({item, userItem}) => {
    console.log('get item, userItem from parent component', item, userItem)

    return (
        <>
            <div></div>
        </>
    )

    
}

export default EditItems
